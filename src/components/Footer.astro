---
/**
 * Footer optimisé SEO - Sans pages villes thin content
 * Conversion-first : téléphone en évidence
 */
import { siteConfig, getPhoneLink, getEmailLink } from '@/data/siteConfig';

const currentYear = new Date().getFullYear();
---

<footer class="bg-primary text-white" role="contentinfo">
  <!-- CTA téléphone dans le footer -->
  <div class="bg-accent py-4">
    <div class="container-custom">
      <div class="flex flex-col sm:flex-row items-center justify-between gap-3">
        <div class="flex items-center gap-3">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          <span class="font-bold text-sm md:text-base">Besoin d'un élagueur ? Appelez maintenant !</span>
        </div>
        <a
          href={getPhoneLink()}
          class="inline-flex items-center gap-2 px-6 py-2.5 bg-white text-accent font-extrabold text-lg rounded-xl hover:bg-neutral-cream transition-all hover:scale-105 shadow-lg"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          {siteConfig.business.phoneDisplay}
        </a>
      </div>
    </div>
  </div>

  <!-- Main footer content -->
  <div class="container-custom py-14 md:py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10 md:gap-12">

      <!-- Colonne 1 : À propos -->
      <div class="space-y-5 lg:col-span-1">
        <div class="flex items-center gap-3">
          <img
            src="/images/logos/Logo abatteur.webp"
            alt="Logo Élagage Abattage 37"
            class="w-14 h-14 object-contain"
            width="56"
            height="56"
            loading="lazy"
          />
          <h3 class="text-xl font-display font-bold">{siteConfig.business.name}</h3>
        </div>
        <p class="text-white/70 text-sm leading-relaxed">
          Votre élagueur professionnel certifié à Tours et en Indre-et-Loire (37). Entreprise familiale avec {siteConfig.stats.yearsExperience}+ ans d'expérience.
        </p>

        <!-- Certifications -->
        <div class="space-y-1.5">
          {siteConfig.business.certifications.map((cert) => (
            <div class="flex items-center gap-2 text-xs text-white/60">
              <svg class="w-3.5 h-3.5 text-accent flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              <span>{cert}</span>
            </div>
          ))}
        </div>

        <!-- Avis Google -->
        <div class="flex items-center gap-2 text-sm">
          <div class="flex text-yellow-400">★★★★★</div>
          <span class="text-white/70">4.9/5 · 127 avis Google</span>
        </div>
      </div>

      <!-- Colonne 2 : Services -->
      <div class="space-y-5">
        <h4 class="text-base font-display font-bold uppercase tracking-wider text-white">Nos services</h4>
        <ul class="space-y-2.5">
          {[
            { name: 'Élagage d\'arbres à Tours', href: '/#services' },
            { name: 'Abattage d\'arbres', href: '/#services' },
            { name: 'Dessouchage', href: '/services/dessouchage-arbres' },
            { name: 'Taille de haies', href: '/#services' },
            { name: 'Entretien espaces verts', href: '/#services' },
            { name: 'Élagage arbres fruitiers', href: '/#services' },
          ].map((link) => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-accent text-sm transition-colors duration-200 inline-flex items-center gap-1.5 group">
                <svg class="w-3 h-3 text-accent/50 group-hover:text-accent transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Colonne 3 : Zones desservies -->
      <div class="space-y-5">
        <h4 class="text-base font-display font-bold uppercase tracking-wider text-white">Zones desservies</h4>
        <div>
          <p class="text-white/60 text-xs mb-3">Élagueur intervenant dans toute l'Indre-et-Loire (37) :</p>
          <div class="flex flex-wrap gap-1.5">
            {siteConfig.serviceArea.cities.slice(0, 12).map((city) => (
              <span class="text-xs px-2 py-1 bg-white/10 text-white/70 rounded-md">
                {city}
              </span>
            ))}
            <span class="text-xs px-2 py-1 bg-accent/20 text-accent rounded-md font-medium">
              +{siteConfig.serviceArea.cities.length - 12} villes
            </span>
          </div>
          <p class="text-white/50 text-xs mt-3">
            Rayon d'intervention : {siteConfig.serviceArea.radius}km autour de Tours
          </p>
        </div>

        <!-- Pages utiles -->
        <div class="pt-2">
          <h5 class="text-sm font-semibold text-white/80 mb-2">Pages utiles</h5>
          <ul class="space-y-1.5">
            {[
              { name: 'Blog élagage', href: '/blog' },
              { name: 'Contact', href: '/contact' },
            ].map((link) => (
              <li>
                <a href={link.href} class="text-white/60 hover:text-accent text-sm transition-colors">
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <!-- Colonne 4 : Contact -->
      <div class="space-y-5">
        <h4 class="text-base font-display font-bold uppercase tracking-wider text-white">Contact</h4>
        <address class="not-italic space-y-4">
          <!-- Téléphone -->
          <a href={getPhoneLink()} class="flex items-start gap-3 text-white/80 hover:text-accent transition-colors group">
            <svg class="w-5 h-5 mt-0.5 flex-shrink-0 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
            </svg>
            <div>
              <p class="font-bold text-base">{siteConfig.business.phoneDisplay}</p>
              <p class="text-xs text-white/50">Disponible 7j/7</p>
            </div>
          </a>

          <!-- Email -->
          <a href={getEmailLink()} class="flex items-start gap-3 text-white/70 hover:text-accent transition-colors">
            <svg class="w-5 h-5 mt-0.5 flex-shrink-0 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <span class="text-sm break-all">{siteConfig.business.email}</span>
          </a>

          <!-- Adresse -->
          <div class="flex items-start gap-3 text-white/70">
            <svg class="w-5 h-5 mt-0.5 flex-shrink-0 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <div class="text-sm">
              <p>{siteConfig.business.address.street}</p>
              <p>{siteConfig.business.address.postalCode} {siteConfig.business.address.city}</p>
              <p>{siteConfig.business.address.region}, France</p>
            </div>
          </div>

          <!-- Horaires -->
          <div class="flex items-start gap-3 text-white/70">
            <svg class="w-5 h-5 mt-0.5 flex-shrink-0 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <div class="text-sm">
              <p class="font-semibold text-white/90">Disponible 7j/7</p>
              <p class="text-xs text-white/50">Urgences : intervention sous 2h</p>
            </div>
          </div>
        </address>
      </div>

    </div>
  </div>

  <!-- Bottom bar -->
  <div class="border-t border-white/10">
    <div class="container-custom py-5">
      <div class="flex flex-col md:flex-row justify-between items-center gap-3 text-xs text-white/50">
        <div class="text-center md:text-left">
          <p>© {currentYear} {siteConfig.business.name}. Tous droits réservés.</p>
          <p class="mt-0.5">SIRET : {siteConfig.business.siret} · Élagueur certifié à Tours (37)</p>
        </div>
        <div class="flex flex-wrap justify-center gap-4">
          <a href="/mentions-legales" class="hover:text-accent transition-colors">Mentions légales</a>
          <a href="/contact" class="hover:text-accent transition-colors">Contact</a>
          <a href="/blog" class="hover:text-accent transition-colors">Blog élagage</a>
        </div>
      </div>
    </div>
  </div>
</footer>
